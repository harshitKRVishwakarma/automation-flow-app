export const adjustNode = (state, node, parentId) => {
	// called when the condition box parent is not root
	console.log("ADJUST NODE CALLED");
	console.log("NODE", node);
	console.log("PARENT_ID", parentId);
};
